sudo: false
language: python

branches:
  except:
  - gh-pages
  - legacy
  - experimental

python:
- "3.6"

install:
- pip install tox-travis
- pip install codecov coverage


script:
- tox

after_success:
- coverage combine && codecov


deploy:
  provider: pages
  skip-cleanup: true
  github-token: a31272f201a9a02094d1afcd1a968e697221f299
  keep-history: true
  on:
    branch: master